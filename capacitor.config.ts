import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.floatingdino',
  appName: 'Floating Dino',
  webDir: 'dist',
  plugins: {}
};

export default config;
